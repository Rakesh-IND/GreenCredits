# Rename to .env and set your values for production.
# For LOCAL development using Vite proxy, leave VITE_API_BASE_URL completely empty!
# For PRODUCTION to Netlify, set it to your remote backend URL without a trailing slash (e.g. https://my-backend.com)
VITE_API_BASE_URL=
