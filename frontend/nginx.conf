server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Support React Router by routing all requests to index.html
        try_files $uri $uri/ /index.html;
    }
}
